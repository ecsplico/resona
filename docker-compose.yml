services:
  whisper-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7000:7000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped

  # GPU variant — uncomment and comment out the above service to use
  # whisper-server-gpu:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.gpu
  #   ports:
  #     - "7000:7000"
  #   volumes:
  #     - ./data:/app/data
  #   env_file:
  #     - .env
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   restart: unless-stopped
